{
  "name": "whylee",
  "version": "6.0.7",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx http-server -c-1 -p 8080 .",
    "serve": "npx http-server -c-1 -p 8080 .",
    "serve:netlifylike": "npx http-server -c-1 -p 8080 . --push-state",
    "export:posters": "node media/scripts/export-images.js",
    "build": "echo \"Static site — nothing to build\"",
    "lint": "echo \"No lint step\""
  },
  "engines": { "node": ">=18" }
}
