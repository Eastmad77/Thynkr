# ─────────────────────────────
#  Whylee App – Environment Template
# ─────────────────────────────
# ⚠️ Copy this file to `.env` (do NOT commit `.env`!)
# All values are placeholders — set real ones in Netlify UI.

# ── Firebase (Frontend Safe Keys) ────────────────
FIREBASE_API_KEY=your_firebase_web_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=000000000000
FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxxxxxxxxxxxx
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ── Firebase Admin (Server Key – Base64 JSON) ─────
# Convert your service account JSON into base64:
# macOS/Linux: base64 -i serviceAccount.json | pbcopy
# Windows (PowerShell): [Convert]::ToBase64String([IO.File]::ReadAllBytes("serviceAccount.json")) | Set-Clipboard
FIREBASE_SERVICE_ACCOUNT=your_base64_encoded_service_account_json

# ── Stripe Configuration ───────────────────────────
STRIPE_SECRET_KEY=sk_live_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_MONTHLY=price_12345monthly
STRIPE_PRICE_YEARLY=price_12345yearly

# ── Optional: Analytics + Logging ──────────────────
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
SENTRY_DSN=https://your_sentry_dsn.ingest.sentry.io/projectid

# ── Netlify (Optional Local Test Overrides) ───────
NETLIFY=true
URL=https://your-site.netlify.app
